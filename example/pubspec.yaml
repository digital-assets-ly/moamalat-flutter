name: moamalat_pay_playground
description: Playground for the digital_assets_moamalat_pay Flutter package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  digital_assets_moamalat_pay:
    path: ../
  flutter_dotenv: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # assets:
    # - .env.dev
    # - .env.prod